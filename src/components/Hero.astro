<section
  id="hero"
  class="relative w-full h-screen flex items-center justify-center overflow-hidden"
>
  <video
    class="absolute inset-0 w-full h-full object-cover z-0 opacity-10"
    autoplay
    muted
    loop
    playsinline
    preload="auto"
    aria-hidden="true"
  >
    <source src="/hero-playback-optimized.mp4" type="video/mp4" />
    <source src="/hero-playback.webm" type="video/webm" />
  </video>
  <!-- Spotlight radial gradient background -->
  <div
    class="pointer-events-none absolute inset-0 flex items-center justify-center z-10"
  >
    <div
      class="radial-bg w-[80vw] h-[80vw] max-w-3xl max-h-3xl rounded-full bg-[radial-gradient(ellipse_at_center,_rgba(255,140,66,0.25)_0%,_rgba(255,140,66,0)_70%)]"
    >
    </div>
  </div>
  <div
    class="relative z-10 flex flex-col items-center justify-center w-full h-full text-center"
  >
    <h1
      class="hero-title text-5xl md:text-[150px] font-semibold drop-shadow-vivid mb-12"
    >
      {
        Array.from("DEV-cadabra!").map((char) =>
          char === " " ? " " : <span>{char}</span>,
        )
      }
    </h1>
    <h2 class="hero-subtitle font-semibold text-3xl">
      The personal website of <span class="text-primary">Ore Adeleye</span>
    </h2>
    <div class="mt-16 flex flex-col items-center w-full px-8">
      <div
        class="nav-box bg-white/10 backdrop-blur-md border border-white/30 rounded-xl shadow-lg px-8 py-6 flex flex-col items-center max-w-2xl w-full"
      >
        <div class="text-lg font-semibold mb-4">Navigate My Journey:</div>
        <div class="flex flex-col md:flex-row gap-4">
          <a href="#past" class="button">Background</a>
          <a href="#skills" class="button">Skills</a>
          <a href="#horizon" class="button">Projects</a>
        </div>
      </div>
    </div>
  </div>
  <div class="absolute inset-0 bg-black/40 z-5"></div>
</section>

<script src="../scripts/hero-animations.ts"></script>
